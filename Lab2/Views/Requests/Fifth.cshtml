@model object
@{
    ViewData["Title"] = "Fifth";
}

@if (Context.Request.Method == "GET")
{
    <div class="container">
        <pre>
            @((Model as Lab2.Controllers.RequestsController.GetViewModel).Query)
        </pre>
        <span>
            @((Model as Lab2.Controllers.RequestsController.GetViewModel).Description)
        </span>
    </div>

    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div class="form-group">
                    <label for="X" class="control-label"></label>
                    <input name="X" type="number" class="form-control" />
                </div>
                <div class="form-group">
                    <input type="submit" value="Execute" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
}

@if (Context.Request.Method == "POST")
{

    var m = (IEnumerable<Song>)Model;
    var q = new Song();
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => q.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => q.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => q.AlbumId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => q.LabelId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => q.MainArtistId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => q.SecondaryArtistId)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in m)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AlbumId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LabelId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MainArtistId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SecondaryArtistId)
                </td>
            </tr>
            }
        </tbody>
    </table>

}